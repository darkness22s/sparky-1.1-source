Persistent Memory
Added persistent Memory support.
Create, search, edit, and delete memories from Settings.
Global memories shared across projects.
Project-scoped memories tied to a workspace.
Memory categories and importance levels.
Memories reused in future conversations and sessions.
Cross-platform memory storage resolution.
Migration and recovery handling for existing memory locations.
Duplicate memories merged by memory ID.
Clear loading, retry, save, update, and delete errors.
Memory failures no longer crash the surrounding session.
Memory management is exposed through both Rust tools and the web Settings UI.
Conversation continuity and sessions
Existing sessions now reload their persisted provider state more reliably.
Follow-up turns continue the same provider conversation instead of silently starting over.
Missing resume state fails loudly instead of creating a fresh hidden session.
Session bindings and transcripts persist under the Sparky session directories.
Session reload regressions were added for legacy provider-state blocks.
Restart-safe provider/session serialization.
Two-turn recall survives reload and compaction.
Opaque provider continuation data is preserved with assistant messages.
GPT-5.6 encrypted reasoning items are captured and replayed correctly.
Provider-specific state is kept hidden from unrelated provider formats and compaction output.
Runtime session loading no longer modifies the user’s Git index.
Incomplete provider streams are no longer treated as successful completed turns.
Turn completion requires a valid user-facing summary.
Removed premature agent turn limits that could terminate longer coding tasks.
OAuth and Codex improvements
Added native /compact support for Codex OAuth sessions.
Context-window overflow now compacts the same provider thread and retries once instead of restarting.
Conservative OAuth context handling prevents incorrectly trusting oversized reported limits.
Per-model OAuth context limits are used when available.
Explicit user-provided context settings are preserved.
Compaction lifecycle events no longer leave the UI stuck in a busy state.
OAuth model discovery is more reliable.
GPT-5.6 conversation continuity is preserved across follow-ups, reloads, and compaction.
Provider and model capabilities
Reasoning-effort options are discovered from provider metadata.
Context-window sizes are discovered from provider metadata.
Supports nested, alternate, numeric, k, and m context formats.
Anthropic and Google model detail endpoints are enriched.
Models.dev metadata is used as a fallback.
Unsupported reasoning levels are no longer fabricated in the UI.
Disabled OpenCode variants are filtered.
Model-picker entries are less duplicated and confusing.
Model capabilities remain provider-authoritative.
Provider session restarts and runtime bindings are handled more safely.
Provider event and turn lifecycle handling is more reliable.
Transient network and connection failures receive better retry handling.
MCP launch arguments are detected from both static and dynamic sources.
MCP model/catalog data reloads when the relevant configuration changes.
New-thread title generation from the first user message was preserved and regression-tested.
Image and multimodal requests
Fixed normal default interaction mode mapping so regular turns use Sparky Build mode.
Prevented image turns from failing before reaching the Rust CLI.
Uploaded image attachments are resolved through the server attachment directory.
Image files are validated before sending.
Images are forwarded as base64 multimodal parts with MIME types.
Image-only prompts receive an effective prompt instead of being discarded.
Rust formatting tests verify image payload forwarding.
The image forwarding thread had strong unit-test coverage, but it did not include a live provider acceptance test.
Rust-native Plan mode
Plan mode is implemented inside Sparky’s Rust harness.
Plan mode uses --interaction-mode plan.
Planning uses a dedicated prompt and read-only tool policy.
ask_user and update_plan tools are available to the planning flow.
Switching back restores Build behavior.
Sparky no longer routes generic Plan mode to OpenCode’s native plan agent.
OpenCode’s Plan toggle was intentionally disabled for this path.
Tool execution and file editing
More reliable edits on Windows and CRLF-formatted files.

Better stale-file detection.

Safer sequential edits using current file snapshots.

Better handling of ambiguous replacement matches.

Improved malformed-argument handling.

Improved incomplete tool-call handling.

Better recovery guidance in tool errors.

More robust handling of large or truncated tool-call payloads.

Improved file-change statistics after tools finish.

Tool lifecycle states now distinguish:
Running
Completed
Failed
Stopped
Declined

Workspace inventory scanning was consolidated so context files and skills can share one scan.

Context summaries and recent history tails are bounded.

Tool protocol repair handles malformed or incomplete tool interactions more defensively.

Streaming performance and appearance
Performance
Adjacent provider deltas are coalesced in a short websocket window.
Growing Markdown is rendered on a roughly 48 ms pacer instead of reparsing on every provider delta.
Immediate streaming publication was preserved while reducing expensive UI commits.
Previously rendered assistant text remains stable during streaming.
Active streaming is scoped to the current turn.
Reduced-motion preferences are respected.
Animation options
The General settings tab now supports a streaming-text animation choice:
Fade + lift — the approved “B” animation.
Word dissolve — the approved “E” animation.
The selection is persisted locally.
Existing installations default to Fade + lift.
Markdown-aware tail animation avoids restarting the whole paragraph.
Only newly arriving text animates.
First and terminal chunks are handled correctly.
Sanitization preserves only approved animation classes.
Visual cleanup
The redundant Working row disappears once visible assistant text is streaming.
The redundant Completed lifecycle label was removed.
Previously rendered text no longer flickers or fades repeatedly.
The streaming animation fix specifically replaced the earlier whole-paragraph opacity restart that caused flicker.
Stop and cancellation behavior
Stop now settles local UI state immediately.
The UI no longer waits several seconds for the provider interrupt RPC before stopping.
Projected streaming assistant messages are finalized immediately.
Late assistant deltas are rejected after interruption.
Late completions are rejected after interruption or session stop.
Provider cancellation still runs concurrently.
Existing provider failure diagnostics are preserved.
activeTurnId is now the authoritative indicator of active work.
Warm provider sessions without an active turn render as ready.
Stop is not sent with an invalid or missing turn ID.
Terminal cleanup and stale-event rejection were strengthened.
Warning and status banners
Non-actionable provider warning-status banners are hidden.
The ChatView client/server version-mismatch banner was removed.
Environment error banners remain visible.
Provider error banners remain visible.
The separate Desktop “Update available” banner was intentionally left untouched.
Added regression tests for the banner stack and provider status behavior.
Desktop notifications
Added native desktop notifications when an agent turn settles.
Windows, macOS, and Linux notification behavior is supported.
Added typed desktop IPC and preload wiring.
Notifications contain the first seven response lines.
Notifications are emitted once per completed turn.
Added focused Electron and web notification tests.
Timeline and tool presentation
Added unique inline icons for built-in tool calls.
Web search now has its own dedicated icon/lifecycle type.
File-change statistics update while work is still in progress.
Timeline statuses better distinguish running, completed, failed, stopped, and declined operations.
Tool feedback is less visually ambiguous.
Web search
Fixed DuckDuckGo HTML result parsing.
Supports protocol-relative result URLs.
Supports redirect-wrapped result URLs.
Normalizes result URLs.
Deduplicates repeated search results.
Handles changed result delimiters.
Produces more generic, provider-neutral user-facing errors.
Updated web-search documentation.
Added regression coverage for the parser and timeline presentation.
See PR #1 and PR #6.
Browser preview automation
Stabilized preview bridge subscriptions.
Added status fallbacks and retry behavior.
Cleaned up interrupted clicks.
Added safer popup URL handling.
Improved evaluate-return handling.
Improved recording cleanup.
Improved in-progress preview/tool feedback.
Added focused preview-manager and bridge regression tests.
See PR #5.
Linux desktop support
Added Linux x64 AppImage packaging.
Added detached AppImage signature handling.
Added Linux signing/publication workflow support.
Updated download-worker platform detection.
Updated analytics release metadata.
Updated the admin release flow.
Updated website download detection and platform presentation.
Preserved Windows and macOS release routing.
See PR #3.
The Linux packaging workflow exists, but the retained Linux installer-build thread was interrupted before producing and verifying a real AppImage artifact.
Branding, documentation, and release surfaces
Replaced the stale startup splash image with the current Sparky logo.
Updated website title and product presentation.
Updated documentation pages and web-search documentation.
Updated website styling.
Preserved existing updater endpoints and compatibility routes.
Rebuilt the Windows desktop installer with 1.1.2 metadata.
Desktop product identity remains Sparky.
